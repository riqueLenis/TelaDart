specVersion: "v1beta"
serviceId: "controle_abastecimento"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "controle_abastecimento-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
